#include <stdio.h>
#include <unistd.h>

/**
 * main - Entry point
 * wait - takes the address of an uninitialized integer
 *        as an argument
 * child - name of a process
 */
int main(void)
{
	int wait child;

	else
	{
		wait (&wait_child);
	}

#include <stdio.h>
#include <unistd.h>

/**
 * main - Entry point
 * wait - takes the address of an uninitialized integer
 *        as an argument
 * child - name of a process
 * Return: Always 0.
 */
int main(void)
{
	int wait child;

	else
	{
		wait(&wait_child);
		return (0);
	}
